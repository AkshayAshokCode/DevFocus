<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.github.akshayashokcode.devfocus</id>
    <name>DevFocus</name>
    <vendor email="akshayashokan1054@gmail.com"
            url="https://github.com/akshayashokcode"
    >akshayashokcode</vendor>
    <description>
        <![CDATA[
        DevFocus is a Pomodoro timer for JetBrains IDEs that keeps developers in flow through
        structured focus sessions and breaks — without leaving the editor.<br/><br/>
        Timer state survives restarts and crashes. Built-in Classic and Deep Work modes, plus
        unlimited named custom presets switchable from the mode dropdown. Live end-time display
        shows exactly when your session ends and when your full round finishes — so you can plan
        your day around your focus blocks. Customizable ring colors, a persistent status bar
        countdown, actionable notifications, and full keyboard action support.
        ]]>
    </description>

    <change-notes>
        <![CDATA[
        <ul>
            <li><b>End time display</b> — live "Ends at X:XX PM" and "Free by X:XX PM" labels appear while the timer runs, hidden when paused</li>
            <li><b>Saved custom presets</b> — name and save your own timing configurations; they appear directly in the mode dropdown</li>
            <li><b>Customizable ring colors</b> — pick Focus and Break accent colors from a color picker in Settings</li>
            <li><b>Icon play/pause toggle</b> — single button replaces separate Start/Pause text buttons</li>
            <li><b>Spinner inputs in Custom mode</b> — step controls with min/max bounds replace free-text fields</li>
        </ul>
        ]]>
    </change-notes>

    <idea-version since-build="233"/>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow factoryClass="com.github.akshayashokcode.devfocus.toolWindow.DevFocusToolWindowFactory"
                    id="DevFocus"
                    icon="/icons/startTimer.svg"
        />
        <projectService serviceImplementation="com.github.akshayashokcode.devfocus.services.pomodoro.PomodoroTimerService"/>
        <notificationGroup id="DevFocus Notifications" displayType="BALLOON"/>
        <statusBarWidgetFactory id="DevFocusStatusBarWidget"
                                implementation="com.github.akshayashokcode.devfocus.statusbar.PomodoroStatusBarWidgetFactory"/>
    </extensions>

    <actions>
        <group id="DevFocus.ActionGroup" text="DevFocus" popup="true">
            <add-to-group group-id="ToolsMenu" anchor="last"/>

            <action id="DevFocus.Toggle"
                    class="com.github.akshayashokcode.devfocus.actions.DevFocusToggleAction"
                    text="Start / Pause Timer"
                    description="Start the DevFocus timer, or pause it if already running"/>

            <action id="DevFocus.Reset"
                    class="com.github.akshayashokcode.devfocus.actions.DevFocusResetAction"
                    text="Reset Timer"
                    description="Reset the DevFocus timer to the start of the current round"/>

            <action id="DevFocus.SkipBreak"
                    class="com.github.akshayashokcode.devfocus.actions.DevFocusSkipBreakAction"
                    text="Skip Break"
                    description="Skip the current break and start the next work session immediately"/>
        </group>
    </actions>
</idea-plugin>
